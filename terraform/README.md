# Terraform para API REST (inicial)

1. `az login` (autenticar Azure CLI)
2. `cd terraform`
3. `terraform init`
4. `terraform plan`
5. `terraform apply -auto-approve`

Para destruir:
`terraform destroy -auto-approve`
